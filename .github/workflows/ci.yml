name: CI

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Basic repository health
        run: |
          git status --short
          echo "Define repo-specific CI checks in this workflow as needed."
